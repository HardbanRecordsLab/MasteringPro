# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files — never commit real secrets / keys
.env
.env.*
!.env.example
server/.env
server/.env.*
!server/.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vercel
.vercel

# Private infrastructure notes / VPS handbook — never commit
# (contains server IPs, DB policy, secrets references). Keep local only.
HBRL-VPS/
