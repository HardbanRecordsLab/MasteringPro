{
  "name": "masteringpro-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MasteringPro API — AI mastering/copilot proxy + accounts, projects and presets (Postgres).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "node src/db/migrate.js",
    "db:selftest": "node scripts/db-selftest.mjs"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "drizzle-orm": "^0.45.2",
    "hash-wasm": "^4.12.0",
    "hono": "^4.6.14",
    "postgres": "^3.4.9",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "drizzle-kit": "^0.31.10"
  }
}
